export type SigninInputModel = {
    /**
     * Set loginOrEmail for searching user by login or email.
     */
    loginOrEmail: string

    /**
     * Set password for checking found user by password.
     */
    password: string
};
